[package]
name = "execra-tauri"
version = "0.1.1"
edition = "2021"
description = "Thin adapter types for forwarding Execra events to Tauri."
license = "Apache-2.0"
repository = "https://github.com/AmarBego/Execra"

[dependencies]
execra = { version = "0.1.1", path = "../.." }
serde = { version = "1", features = ["derive"] }
